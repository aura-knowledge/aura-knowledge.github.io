---
import type { ArticleArtifact } from "../lib/content";

interface Props {
  sources: ArticleArtifact["sources"];
}

const { sources } = Astro.props;
---

<details class="source-ledger" aria-labelledby="sources-title">
  <summary>
    <span>
      <span class="eyebrow">Structured sources</span>
      <strong id="sources-title">Source ledger</strong>
      <small>{sources.length} source{sources.length === 1 ? "" : "s"}</small>
    </span>
    <span class="details-state">
      <span class="when-closed">Show sources</span>
      <span class="when-open">Hide sources</span>
    </span>
  </summary>
  <ul>
    {sources.map((source) => (
      <li>
        <a href={source.url}>{source.title}</a>
        <span>{source.type}</span>
      </li>
    ))}
  </ul>
</details>
